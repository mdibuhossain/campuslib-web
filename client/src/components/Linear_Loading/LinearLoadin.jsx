import { LinearProgress } from '@mui/material';
import React from 'react';

const LinearLoadin = () => {
    return (
        <div>
            <LinearProgress sx={{ height: 6 }} />
        </div>
    );
};

export default LinearLoadin;